# ── HemoLink Frontend Environment ────────────────────────────────────────────
# Copy this file to .env.local and fill in your values.
# .env.local is ignored by git.

# Backend API URL — change this when deploying
VITE_API_URL=http://localhost:3001/api
